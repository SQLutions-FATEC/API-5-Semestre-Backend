apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090   # <-- MUDAR AQUI (antes devia estar http://prometheus:9090)
    isDefault: true
    editable: true